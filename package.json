{
  "name": "pdl",
  "version": "0.1.0",
  "private": true,
  "description": "PDL (Programmatic Design Language) compiler toolchain",
  "type": "module",
  "bin": {
    "pdl": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "renderHtmlFromBake": "tsc && node dist/cli.js renderHtml --from-bake",
    "preview": "tsc && node scripts/preview-server.mjs",
    "renderCatalogueHtml": "tsc && node dist/cli.js renderCatalogueHtml",
    "manifest": "tsc && node dist/cli.js manifest",
    "playground": "npm run build && npm run build:rust && npm run build --prefix playground && node playground/server/playground-server.mjs",
    "playground:fresh": "npm run build && npm run build:rust && npm run build:wasm && npm run build --prefix playground && node playground/server/playground-server.mjs",
    "studio": "npm run build && npm run build:rust && npm run build --prefix apps/studio && node apps/studio/server/studio-server.mjs",
    "studio:fresh": "npm run build && npm run build:rust && npm run build:wasm && npm run build --prefix apps/studio && node apps/studio/server/studio-server.mjs",
    "build:wasm": "./scripts/build-pdl-wasm.sh",
    "build:rust": "cargo build -p pdl-cli",
    "pdl:rust": "cargo run -q -p pdl-cli --",
    "test": "npm run build && npm run build:rust && vitest run",
    "test:watch": "vitest",
    "test:rust": "cargo test -p pdl-core",
    "docs:gen": "node scripts/gen-docs.mjs",
    "docs:dev": "npm run docs:gen && npm run dev --prefix website",
    "docs:build": "npm run docs:gen && npm run build --prefix website"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "happy-dom": "^20.11.2",
    "typescript": "^5.8.2",
    "vitest": "^3.1.1"
  }
}
