import "../atoms/tokens_color.pdl"

variant V { case a }

component Real(v: V = .a) layout {
  direction = .column
  children = []
}

usage NoSuchComponent {
  description = "references a component that does not exist"
}
