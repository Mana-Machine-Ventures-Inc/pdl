semantic motion.pop: Animation = Animation(
  keys: [Motion(duration: 200, ease: .out, pose: Pose(scale: 1.1))]
)

component BadMerge <PointerInput>() layout {
  children = []
  self.appear = {
    animate = Animation(motion.pop, pose: Pose(opacity: 0))
  }
}
