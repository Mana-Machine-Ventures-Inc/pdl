component BadEasing <PointerInput>() layout {
  children = []
  self.appear = {
    animate = Animation(
      keys: [
        Motion(duration: 200, ease: .out, pose: Pose(scale: 1.12), easing: .out)
      ]
    )
  }
}
