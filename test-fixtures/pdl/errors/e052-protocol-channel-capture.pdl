// PDL-E052: Protocol.channel is not ancestor capture.

variant EpisodeId { case demo }

protocol ShowEpisode: component {
  emits(propagation: .ancestors) {
    showEpisode(id: EpisodeId)
  }
}

screen Phone() layout {
  children = []
  ShowEpisode.showEpisode(id: EpisodeId) = { }
}
