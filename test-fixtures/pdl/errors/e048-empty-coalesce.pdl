// PDL-E048: a mount let/assign coalesce with no fallback produced no value.

variant WindowSize {
  case compact
  case medium
}

host Default(sizeClass: WindowSize = .medium) mount {
  self.sizeClass = host["missing"] as? WindowSize
}

component Shell <Host>() layout {
  children = []
}
