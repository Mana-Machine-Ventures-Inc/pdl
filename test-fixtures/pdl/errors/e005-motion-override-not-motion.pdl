primitive color.red: Color = #FF0000

component BadCopy <PointerInput>() layout {
  children = []
  self.appear = {
    animate = Animation(color.red, keys: [
      Motion(duration: 200, ease: .out, pose: .rest)
    ])
  }
}
