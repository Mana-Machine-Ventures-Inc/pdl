import "../atoms/tokens_color.pdl"

variant V { case a }

component Real(v: V = .a) layout {
  direction = .column
  children = []
}

// `interaction` blocks were removed — this must fail to parse (PDL-E001).
interaction X for Ghost {
  on hoverStart {
    v = .a
  }
}
